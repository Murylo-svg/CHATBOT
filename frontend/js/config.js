/* ============================================================
   ÚNICO ARQUIVO QUE VOCÊ PRECISA EDITAR AO PUBLICAR.
   Troque pela URL do seu backend no Render (sem barra no final).
   ============================================================ */

const API_URL = "http://localhost:3000";

// Exemplo em produção:
// const API_URL = "https://agente-ia-backend.onrender.com";
